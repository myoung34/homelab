apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: benthos
  namespace: benthos
  annotations:
    tailscale.com/funnel: "true"
spec:
  defaultBackend:
    service:
      name: benthos
      port:
        number: 80
  ingressClassName: tailscale
  tls:
  - hosts:
    - benthos
