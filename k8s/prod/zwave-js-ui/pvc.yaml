apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zwave-js-ui
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 128Mi
  volumeMode: Filesystem
  volumeName: zwave-js-ui
