apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-ngx
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 128Mi
  volumeMode: Filesystem
  volumeName: paperless-ngx
